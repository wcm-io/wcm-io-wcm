{
  "jcr:primaryType": "nt:unstructured",
  "query": "${param.query}",
  "sling:resourceType": "granite/ui/components/coral/foundation/form/pathfield/suggestion",
  "datasource": {
    "filter": "${paramValues.filter}",
    "offset": "${requestPathInfo.selectors[0]}",
    "rootPath": "${empty param.root ? \"/\" : param.root}",
    "limit": "${empty requestPathInfo.selectors[1] ? \"10\" : requestPathInfo.selectors[1]}",
    "query": "${param.query}",
    "sling:resourceType": "wcm-io/wcm/ui/granite/components/form/pathfield/datasources/children"
  }
}
